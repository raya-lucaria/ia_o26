course_id: ia_o26
title: "Inteligencia Artificial \u2014 Oto\u00f1o 2026 (ITAM)"
description: "Curso de Inteligencia Artificial del ITAM, Oto\u00f1o 2026."
language: "es"
source: course
artifact: artifact
calendar:
  timezone: America/Mexico_City
render:
  skin: eva-cyberpunk
  # Estilo de resaltado de código. El de Pygments por omisión es CLARO, así que
  # sobre el fondo oscuro de eva-cyberpunk el código salía ilegible. Va aquí y
  # no por idioma porque es una decisión de piel: el builder no puede saber si
  # un skin es oscuro.
  #
  # Las etiquetas de callouts y de entornos estáticos —«Nota», «Respuesta a»—
  # NO se configuran aquí: el builder las localiza con el campo `language` de
  # arriba.
  code_style: monokai
  numbered_objects:
    numbering: page-hierarchy
    sequences:
      figure: { label: "Figura", style: caption }
      table: { label: "Tabla", style: caption }
      theorem: { label: "Teorema", style: scannable }
      example: { label: "Ejemplo", style: scannable }
      exercise: { label: "Ejercicio", style: scannable }
    families:
      figure: { sequence: figure, label: "Figura" }
      table: { sequence: table, label: "Tabla" }
      theorem: { sequence: theorem, label: "Teorema" }
      definition: { sequence: theorem, label: "Definición" }
      remark: { sequence: theorem, label: "Observación" }
      example: { sequence: example, label: "Ejemplo" }
      exercise: { sequence: exercise, label: "Ejercicio" }
